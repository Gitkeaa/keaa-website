node_modules
dist
dist-ssr
*.local

.DS_Store
.env
.env.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local dev logs
*.log
devserver.log

# throwaway scratch scripts (screenshot runners etc) — regenerated on demand
*.tmp.mjs

.vercel
.env*

# Run artefact from scripts/capture-video-posters.mjs — regenerated on every run.
scripts/video-posters.report.json

# Review artefacts for the translation and product-description batches. These are working
# files for the team (drafts, CSVs, per-language review sheets), regenerated by
# scripts/gen-translation-review.mjs and the description generator. The reviewed wording
# lands in src/i18n/products/*.json and src/data/products.json; the sheets themselves are
# not part of the site and must not bloat the repo.
product-descriptions-review/
translation-review/
